1. this demo use unslider plugin, and JQuery for slider.

2. simply open the index.html with browser.

( because task are required in static web page. no 'npm task' or 'gulp' automation building has been configured. )